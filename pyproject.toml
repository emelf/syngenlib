[build-system]
requires = ["setuptools"] 
build-backend = "setuptools.build_meta"

[project]
name = "syngenlib"
version = "0.1.0"
requires-python = ">=3.10.0"

dependencies = [
    "numpy",
    "matplotlib", 
]